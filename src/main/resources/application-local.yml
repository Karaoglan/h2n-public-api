spring:
  data:
    mongodb:
      database: test
      port: 27017
  mongodb:
    embedded:
      version: 4.0.2

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG